name: Build and Deploy to Droplet

on:
  push:
    branches: ["main"]

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Log in to DigitalOcean Container Registry
        run: echo ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }} | docker login registry.digitalocean.com -u ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }} --password-stdin

      - name: Build and Push
        run: |
          # Target is 'runner' to match your Dockerfile
          docker build --target runner -t registry.digitalocean.com/${{ secrets.REGISTRY_NAME }}/opletics-web:latest .
          docker push registry.digitalocean.com/${{ secrets.REGISTRY_NAME }}/opletics-web:latest

  deploy:
    needs: build_and_push
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Droplet via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Authenticate doctl on the droplet
            doctl auth init --access-token ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}
            doctl registry login --expiry-seconds 1200

            # Pull the fresh image
            docker pull registry.digitalocean.com/${{ secrets.REGISTRY_NAME }}/opletics-web:latest

            # Cleanup old container
            docker stop opletics-web || true
            docker rm opletics-web || true

            # Start new container
            docker run -d \
              --name opletics-web \
              --restart always \
              -p 3000:3000 \
              registry.digitalocean.com/${{ secrets.REGISTRY_NAME }}/opletics-web:latest
