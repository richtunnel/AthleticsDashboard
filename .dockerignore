# ============================================
# Docker build context exclusions tailored for this repo
# ============================================

# Node dependencies (reinstalled inside container)
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Yarn 1 cache and state files
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/patches
!.yarn/versions

# Next.js build artifacts
.next
out
build
dist

# TypeScript build info
*.tsbuildinfo

# Environment files (use runtime ENV instead)
.env*
!.env.example
!.env.docker

# Testing artifacts
coverage
__tests__
__mocks__
.jest
**/*.test.*
**/*.spec.*

# IDE/editor files
.vscode
.idea
*.swp
*.swo
*.iml

# OS-specific
.DS_Store
Thumbs.db
desktop.ini

# Git metadata
.git
.gitignore
.gitattributes

# Documentation (not needed during build)
README.md
*.md
!DOCKER.md

# Deployment configs handled separately
cloudbuild.yaml
nixpacks.toml
.dockerignore
.dockerignore.local
.do
railway.json

# Misc
mock_data.json
testhandler.ts
routes-manifest.json
